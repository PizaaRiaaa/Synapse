<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/white"
    tools:context=".screen.PickRole">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:orientation="horizontal">

        <com.google.android.material.button.MaterialButton
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:id="@+id/btnPickCarer"
            android:layout_width="0dp"
            android:layout_height="200dp"
            android:layout_weight="1"
            android:layout_margin="20dp"
            app:backgroundTint="@color/et_bgTint"
            app:strokeColor="@color/et_stroke"
            app:strokeWidth="3dp"
            android:textColor="@color/dark_grey"
            android:text="@string/carer"/>

        <com.google.android.material.button.MaterialButton
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:id="@+id/btnPickSenior"
            android:layout_width="0dp"
            android:layout_height="200dp"
            android:layout_weight="1"
            android:layout_margin="20dp"
            android:textAlignment="center"
            app:backgroundTint="@color/et_bgTint"
            app:strokeColor="@color/et_stroke"
            app:strokeWidth="3dp"
            android:textColor="@color/dark_grey"
            android:text="@string/senior_citizen"/>


    </LinearLayout>

</LinearLayout>